# hackathon-lp-2025 — static site hosted on Cloudflare Workers (Static Assets).
# Assets-only Worker (no `main`): Cloudflare serves ./public directly.
#
# Deployed via Cloudflare Workers Builds (Git-connected — NOT GitHub Actions, so no
# Cloudflare token is stored in GitHub). Configure this Worker once in the Cloudflare
# dashboard (Workers & Pages → connect the nemtus/apps repo) with:
#   Root directory:    apps/hackathon-lp-2025
#   Build command:     (none — static)
#   Deploy command:    npx wrangler deploy
#   Build watch paths: apps/hackathon-lp-2025/*   (so only this site rebuilds)
#   Production branch: main   (PRs get preview URLs; ensure Preview URLs are enabled)
name = "hackathon-lp-2025"
compatibility_date = "2025-06-01"

[assets]
directory = "./public"
not_found_handling = "404-page" # serve /404.html for unmatched paths (multi-page static)

# Custom domain (later, once DNS is on Cloudflare):
# routes = [{ pattern = "hackathon-2025.nemtus.com", custom_domain = true }]
