# hackathon-lp-2023 — static site hosted as a Cloudflare Worker (Static Assets).
# Assets-only Worker (no `main`): Cloudflare serves ./public directly.
name = "hackathon-lp-2023"
compatibility_date = "2025-06-01"

[assets]
directory = "./public"
not_found_handling = "404-page"

# Attach the custom domain after the first deploy / once DNS is on Cloudflare
# (verify the exact hostname used for 2023):
# routes = [{ pattern = "hackathon-2023.nemtus.com", custom_domain = true }]
