# pnpm reads parent .npmrc files, and the repo-root .npmrc sets ignore-scripts=true
# (supply-chain hardening for the npm workspaces). This Next.js app is a standalone pnpm
# project whose build needs lifecycle scripts, so override that here.
ignore-scripts=false
