# Supply-chain hardening (inherited from the source repos).
# Block lifecycle install scripts from transitive dependencies to mitigate
# npm ecosystem poisoning. If a legitimate dependency needs to build native
# code, run `npm rebuild <pkg>` explicitly.
ignore-scripts=true
# Pin newly added dependencies to exact versions.
save-exact=true
# Default local audit threshold.
audit-level=high
