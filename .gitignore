# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
build/
dist/
coverage/

# Env / secrets (non-secret Firebase web config in apps may be committed per that app's rules)
.env.local
.env.*.local
*.encoded
*.decoded

# Firebase
.firebase/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cloudflare Workers local state: wrangler's dev cache + the local D1 sqlite created
# by `wrangler d1 ... --local` / `wrangler dev`. Also local secret files.
.wrangler/
.dev.vars
.dev.vars.*

# OS / editor
.DS_Store

# Claude Code (settings.json / .mcp.json / hooks / CLAUDE.md are committed; local overrides are not)
.claude/settings.local.json
